package com.example.fizikaforall.electricity

import com.example.fizikaforall.manual.ManualProject
import java.util.*

/*
class Cable {
    private var cable =  mutableListOf<Resistor>()

    fun getCable():List<Resistor> = cable
    fun setComponent( resistor: Resistor)
    {
        cable.add(resistor)
    }

    fun delComponent(cableResistor:Resistor)
    {
         val indexToDelete =  cable.indexOfFirst { it.id == cableResistor.id }
        if (indexToDelete!= -1) {
            cable.removeAt(indexToDelete)
        }
    }
    fun getAllResistance():Resistance= Resistance(cable.map{it.resistance.resistance}.sum())
}*/
